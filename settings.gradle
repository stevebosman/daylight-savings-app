pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/stevebosman/sunrise-equation")
            credentials {
                username = user ?: System.getenv("GITHUB_ACTOR")
                password = key ?: System.getenv("PAT")
            }
        }
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/stevebosman/timepreference")
        }
    }
}
rootProject.name='Daylight'
include ':app'
